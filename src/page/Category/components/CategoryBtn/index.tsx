import React from "react";
import { Button } from "@mui/material";

const CategoryBtn = () => {
  return (
    <Button variant="outlined" sx={{ width: 160, height: 80, borderRadius: 4 }}>
      카테고리
    </Button>
  );
};

export default CategoryBtn;
