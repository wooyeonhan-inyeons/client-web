export interface UserState {
  role: string;
  id: number;
  name: string;
  first: boolean;
}

export interface menuProps {
  key: string;
  value: string;
}
export interface ThemeProps {
  theme?: Theme;
}
